{
  "name": "twitter-apify-railway-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Railway-deployable proxy that adapts prediction-market Twitter fetch requests to Apify and normalizes tweet-like dataset items.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
